// Copy to Secrets.local.xcconfig (gitignored) and paste the anon key
// from supabase/.env.local. xcconfig treats // as a comment, so URLs
// must use https:/$()/host.

SUPABASE_URL = https:/$()/wtcdyfcqtajzpuwpeekf.supabase.co
SUPABASE_ANON_KEY = REPLACE_ME
